# AGENTS.md

Codex and other agents: the canonical instructions for this repo live in `CLAUDE.md`. Read `CLAUDE.md` before doing any work here; it carries the context, brand sources, and rules that apply, and points to the ZPT business folder for company context.

Global conventions also apply: no em dashes or double dashes as parenthetical separators; use "Wybe" and "Claude", never pronouns; do not create README.md files.

After a big structural change, update `CLAUDE.md` here and `~/Sites/SYSTEM-MAP.md` so both stay current. Codex does not auto-read or auto-update `CLAUDE.md`, so do it explicitly.
